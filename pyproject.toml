[project]
name = "aoer-work-projects"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "crawl4ai>=0.8.0",
    "httpx>=0.28.1",
    "markdown>=3.10.2",
    "markitdown[all]>=0.1.5",
    "weasyprint>=68.1",
]
